language: java
jdk:
  - openjdk8

branches:
  only:
    - master

# Travis CI 서버의 Home
cache:
  directories:
    - '$HOME/server/.m2/repository'
    - '$HOME/server/.gradle'

script:
  - 'cd ./server'
  - 'chmod +x ./gradlew'
  - './gradlew clean build'